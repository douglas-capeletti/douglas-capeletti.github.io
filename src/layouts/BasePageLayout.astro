---
import BaseHead from "../components/BaseHead.astro";
import Nav from "../components/Nav.astro";

export interface Props {
  title: string;
  disableNav?: boolean;
}

const { title, disableNav } = Astro.props;
const seoTitle = `${title} | DevelopingNotes.com`;
---

<html lang="en" class="dark">
  <head>
    <BaseHead title={seoTitle} />
  </head>

  <body class="bg-bg-primary text-text-primary font-body leading-normal personality-casual">
    {!disableNav && <Nav />}
    <main class="py-12 lg:py-20">
      <slot />
    </main>
  </body>
</html>
